    CONSTRAINT check_coach_capacity 
    CHECK (capacity >= 20 AND capacity <= 200)

    CONSTRAINT reg_plate_regex
    CHECK (REGEXP_LIKE(registrationplate, '[A-Z][A-Z][0-9][0-9][" "][A-Z][A-Z][A-Z]'))