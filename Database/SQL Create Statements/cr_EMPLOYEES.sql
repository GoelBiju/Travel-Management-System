CREATE TABLE EMPLOYEES
(
    employee_id         VARCHAR(112) NOT NULL PRIMARY KEY,
    first_name          VARCHAR(112) NOT NULL,
    last_name           VARCHAR(112) NOT NULL,
    job_role            VARCHAR(112) NOT NULL,
    password            VARCHAR(112) NOT NULL     
);